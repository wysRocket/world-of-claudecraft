# Phase 1 (impl) starter prompt: Design-token system + dark-fantasy palette

Paste the fenced block below into a fresh Claude Code session. It is self-contained:
it tells the session what to read, how to orchestrate, what to keep invariant, how to
validate, and when to stop. Do not paste the surrounding prose.

### Starter Prompt

```
This is Phase 1 of the HUD Visual + UX + Accessibility feature: Design-token system + dark-fantasy palette.

Model: Opus 4.8, max effort, 1m-context variant where the file load demands it. Harness: Claude Code.
ULTRACODE: this phase is moderate (two parallel agents, one CSS surface plus one TS/canvas surface, no broad event fan-out), so ultracode is optional. Add ultracode only if you choose to orchestrate the two agents and the validation matrix through a Workflow; a plain two-agent fan-out is sufficient. Do not over-decompose a two-file change.

GOAL (one sentence): Formalize the existing ad-hoc :root CSS custom properties into a primitive/semantic token system, define the premium dark-fantasy semantic palette (dark slate, restrained gold trim, parchment, crest), migrate QUALITY_COLOR to --quality-* tokens that feed the DOM color-string sites (and reconcile the divergent .q-* quality CSS classes onto those tokens) via a CACHED readToken() for any JS site that builds an inline color string, and scaffold a --text-scale multiplier, all WITHOUT changing any rendered color at this stage EXCEPT the one deliberate .q-* reconciliation below. NOTE: the canvas icon painter does NOT read QUALITY_COLOR for the quality border (icons.ts ~:1331 comments that the quality border lives in CSS outside the painter); every quality-color consumer is a DOM color string or a .q-* CSS class, so tokens feed the DOM, not the painter.

STEP 0 - PRE-FLIGHT (do this before anything else; STOP on any failure):
1. Shared worktree. Run `git status`. This tree is shared across concurrent sessions. If it is dirty with files you did not create, STOP and ask the human before touching anything; do not stash or revert another session's work.
2. CROSS-PACKET CHECKPOINT (from state.md "Cross-packet dependency"). Phase 1 (tokens) is explicitly cleared to start BEFORE the ui-architecture-hud-modularization refactor completes: it is CSS plus the DOM color-string sites in hud.ts that consume QUALITY_COLOR (the canvas painter does NOT read quality color), with no modular-seam dependency. Confirm only that the existing :root CSS block and src/ui/icons.ts QUALITY_COLOR are present (the Explore agent does this in Step 1). There is NO HudContext requirement for this phase. If you discover this phase has somehow been made to depend on a refactor seam that has not landed, STOP and surface it.
3. MEMORY SCAN. Read /Users/fernando/.claude/projects/-Users-fernando-Documents-world-of-claudecraft/memory/MEMORY.md and the notes it indexes for these topics: hud, i18n, shared-worktree, never-push-to-fork, no-em-dashes-or-emojis, live-site-and-test-creds. Honor every one. Key ones for this phase: never push to the fork (push to origin levy-street only); stage only your own files with explicit paths (never git add -A); no em dashes or emojis anywhere; the i18n runtime reads a generated resolved file, so do not hand-edit generated i18n outputs.
4. BRANCH. Create and switch to the phase branch: `git switch -c feature/phase-01-design-token-system`. One card, one branch, one PR.

STEP 1 - LOAD CONTEXT (do NOT read the planning docs or src/ui/hud.ts directly in the main loop; src/ui/hud.ts is ~6,280 lines and will blow your context). Spawn ONE Explore agent and have it return a tight written summary (no file edits) of:
- state.md: the Locked decisions, the Non-negotiable invariants (especially 3, 5, 2, 8), the Validation matrix row for "Visual/token change", the Review-dispatch matrix, the Cross-packet dependency note for Phase 1, and the Key file paths + the ledger lines this phase fills.
  Path: /Users/fernando/Documents/world-of-claudecraft/docs/hud-ux-and-accessibility/state.md
- research-brief.md SECTION 5 in full (Visual design-token and theming architecture): the 2.5-tier model (primitive / semantic / thin component), the example kebab-case CSS custom property names, the "name for why, not what" rule, the exact classic QUALITY_COLOR values (poor #9d9d9d, common #ffffff, uncommon #1eff00, rare #0070dd, epic #a335ee), the cached readToken('--quality-epic') recommendation with getComputedStyle invalidated on theme change, the --text-scale multiplier guidance (rem-based, calc(var(--size-3) * var(--text-scale)), set once on change, never per frame), and the classic-fantasy aesthetic notes (dark slate surfaces, restrained gold trim, parchment, crest, Cinzel display + Alegreya Sans body). Also have it skim section 8 for the per-frame budget + cached readToken + extend-dedup-to-aria constraint.
  Path: /Users/fernando/Documents/world-of-claudecraft/docs/hud-ux-and-accessibility/research-brief.md
- progress.md: the "Phase 1 - Design-token system + dark-fantasy palette" deliverable + acceptance checklist (mirror it exactly in Step 5).
  Path: /Users/fernando/Documents/world-of-claudecraft/docs/hud-ux-and-accessibility/progress.md
- This phase prompt file itself: /Users/fernando/Documents/world-of-claudecraft/docs/hud-ux-and-accessibility/phase-01-design-token-system.md
- The SOURCE FILES, read individually and summarized (anchors drift, so RE-GREP the symbol, do not trust line numbers):
  - The :root CSS custom-property block. Re-grep `grep -n ":root" index.html` (currently around index.html:96). Summarize the full current flat token set: --gold and the documented "single source of truth for the accent" comment, --color-primary, --color-hp/mana/rage/energy, --color-bg-*, --color-border-*, --color-text-* (note --color-text-muted), spacing/radius/transition tokens, and the font stack including --font-display: 'Cinzel'. Report whether the :root lives in index.html only or also in a CSS file under src/ui.
  - src/ui/icons.ts: re-grep `grep -n "QUALITY_COLOR" src/ui/icons.ts` (export currently around :1358). Capture the literal map AND every consumer call site (re-grep `grep -rn "QUALITY_COLOR" src/`). IMPORTANT: the canvas icon painter does NOT read QUALITY_COLOR for the quality border; re-grep around icons.ts ~:1331 and confirm the comment that the quality border lives in CSS OUTSIDE the painter (the painter draws from PALETTES + FX glow/sparkle). The actual QUALITY_COLOR consumers are the ~8 DOM color-string sites in src/ui/hud.ts (style="color:..." / border-color strings) plus the static .q-* CSS classes in index.html. Summarize each DOM site and each .q-* class so Agent B knows readToken must return a hex string identical to today's for those DOM sites. (If you find a canvas tint that genuinely reads a JS color, note it separately; quality color specifically is DOM/CSS, not the painter.)
  - index.html .q-* quality CSS classes: re-grep `grep -n "q-common\|q-poor\|q-uncommon\|q-rare\|q-epic" index.html` (currently around index.html:734-738). Capture the exact per-class colors and note the divergence from QUALITY_COLOR (e.g. .q-common is #b8b8b8 here but QUALITY_COLOR common is #ffffff). Report every divergence so Agent A/B can reconcile to ONE chosen value.
  - tests/hud_harness.test.ts and tests/hud_perf_budget.test.ts (from the refactor packet): summarize what they assert (the perf gate enforces a hot-write skip rate above ~0.8) so the change keeps them green.
Spawn a web-research agent ONLY if a current external fact not already in research-brief.md is needed; it is not expected here (section 5 already has the token taxonomy and the exact hex values).

STEP 2 - CHOOSE ORCHESTRATION + EXECUTE. Fan out TWO implementation agents in parallel. Give EACH agent ONLY the Explore summary from Step 1 (not the raw docs, not hud.ts). Request the fan-out explicitly. Use `isolation: "worktree"` only if both agents would edit the same file; here Agent A owns CSS and Agent B owns TS, so the surfaces are disjoint and no worktree isolation is needed (but Agent B does add a new src/ui token-read helper that A does not touch).

Agent A - "primitive + semantic token taxonomy + dark-fantasy palette". Deliverables:
- Promote the existing flat :root set into a deliberate primitive -> semantic layering per research-brief.md section 5, in the SAME place the :root currently lives (do not move the cascade root; keep all existing variable NAMES as semantic aliases so nothing that references them breaks).
- TIER 1 PRIMITIVES (the only place raw hex/px/rem live): a palette ramp (gold ramp anchored on the current --gold value, an ink/slate ramp anchored on the current dark bg, a parchment warm fill, a crest accent), size/space/radius/duration/ease primitives in rem where they feed type so zoom and --text-scale both apply.
- TIER 2 SEMANTIC (intent; "name for why, not what"): --color-accent (alias var(--gold) preserved for back-compat), --color-bg-surface, --color-text-default, --color-text-muted, --color-resource-health/mana/rage/energy, --color-unit-hostile/friendly, --color-aura-buff/debuff, --font-display ('Cinzel', ...), --font-ui body, and the --text-scale: 1 multiplier scaffold (defined and resolvable, NOT yet wired to any control; wiring is Phase 21).
- TIER 2.5 COMPONENT (only where genuinely overriding intent), defined but Agent B fills the --quality-* values: leave the --quality-* slot reserved per the agreed name set so A and B do not collide.
- HARD CONSTRAINT: every semantic value must resolve to the EXACT color rendered today. This phase is token PLUMBING, not a recolor. The dark-fantasy palette is expressed as primitives + semantic intent names, but the resolved pixel for every existing usage stays identical. If introducing the layering would shift any current color, STOP (see stopping rules).
- Do NOT rename any structural DOM id or class (invariant 3); restyle is out of scope this phase anyway (per-surface passes are Phases 7 and 9-18).

Agent B - "QUALITY_COLOR -> --quality-* migration (DOM sites) + reconcile .q-* CSS + cached readToken() helper + tests/tokens.test.ts". Deliverables:
- Define --quality-poor/common/uncommon/rare/epic semantic-component tokens at the same :root. Anchor them on the current QUALITY_COLOR map (poor #9d9d9d, common #ffffff, uncommon #1eff00, rare #0070dd, epic #a335ee). Coordinate the exact token names with Agent A's reserved slot.
- Migrate the ~8 DOM color-string sites in src/ui/hud.ts that read QUALITY_COLOR (style="color:..." / border-color strings) to obtain rarity hex from readToken('--quality-<tier>') instead of the JS literal. These DOM sites are the real consumers; the canvas icon painter is NOT one of them (it draws the quality border from CSS outside the painter, icons.ts ~:1331, and paints from PALETTES + FX glow/sparkle). Keep QUALITY_COLOR exported if other modules still import it (re-grep to confirm), OR have it derive from the tokens, whichever keeps every caller green and the colors identical. The DOM color strings and the .q-* CSS must read ONE source of truth.
- UNIFY THE DIVERGENT .q-* QUALITY CSS CLASSES (DELIBERATE VISUAL CHANGE). The static .q-* classes in index.html (~:734-738; re-grep) diverge from QUALITY_COLOR: e.g. .q-common is #b8b8b8 there but the JS map common is #ffffff. Reconcile both surfaces onto the single --quality-* tokens and pick ONE value per tier. For the diverging common tier, choose the classic convention: common = white (#ffffff). Migrate the .q-* classes to reference var(--quality-<tier>). This is the ONE intended pixel change in this phase, so it must be re-baselined with a REVIEWED Playwright diff (call it out explicitly in the report; it is NOT covered by the zero-delta byte-identity rule that governs the rest of the phase). Every other tier whose .q-* value already matches QUALITY_COLOR stays byte-identical.
- Add a tiny CACHED readToken(name) helper in src/ui (e.g. src/ui/tokens.ts) that reads getComputedStyle(root).getPropertyValue(name).trim() ONCE and memoizes it, exposing an invalidate() (or invalidateTokenCache()) that clears the cache. Invalidation happens ONLY on a theme change (no theme switcher exists yet; just expose the hook for Phase 21). NEVER call getComputedStyle on the per-frame path. The helper feeds the JS-built inline DOM color strings; if any canvas tint genuinely reads a JS color it may use it too, but quality color is DOM/CSS.
- tests/tokens.test.ts: assert (a) each --quality-* token resolves to the chosen hex (common = #ffffff after reconciliation; the others to their historical QUALITY_COLOR hex), (b) readToken returns that same hex and is memoized (a spy on getComputedStyle is called once across repeated reads, and again after invalidate()), (c) the DOM rarity color strings are sourced from the tokens (snapshot the five hex strings), and (d) the .q-* classes and the DOM color strings now resolve to ONE value per tier. If the harness provides a jsdom root, read tokens off it; if jsdom cannot resolve var() chains, test readToken against a stubbed computed style and assert the mapping table separately. Do not weaken the determinism or import invariants.

After both agents return, integrate, run `npx tsc --noEmit`, and reconcile the shared :root edits (A and B both append to one block; resolve any overlap by hand, keep one coherent block).

INVARIANTS THIS PHASE MUST KEEP (cited by number from state.md; only those in play):
- Invariant 3 (DOM id/class contract): the ~214 structural ids that index.html/CSS depend on stay stable. Do visual work through tokens/classes, NEVER by renaming structural ids.
- Invariant 5 (per-frame budget): any JS token read goes through the CACHED readToken(); NEVER call getComputedStyle per frame; add no per-frame allocations; the hot-write skip rate stays above ~0.8 (tests/hud_perf_budget.test.ts must stay green). Note: quality color is consumed by DOM color strings, not the canvas painter, so there is no per-frame getComputedStyle risk on the canvas for QUALITY_COLOR to solve; the cached readToken simply keeps the JS DOM-string sites off getComputedStyle.
- Invariant 2 (t()-only render sink): this phase adds no new visible label, but if any token work surfaces a visible string it MUST be a t() key in en (src/ui/i18n.en.ts), with numbers via formatNumber/formatMoney/Intl. Keep tests/localization_fixes.test.ts and tsc green.
- Invariant 8 (no em dashes or emojis): none in any token name, comment, test, or doc.
- DELIBERATE VISUAL CHANGE NOTE (locked decision 7): this packet re-baselines Playwright snapshots on purpose with a reviewed diff. For THIS phase the intended visual delta is ZERO (pure plumbing) for everything EXCEPT the one .q-* quality-class reconciliation in Agent B (common moving to #ffffff and the .q-* classes pointing at var(--quality-*)). That single reconciliation IS an intended re-baseline with a reviewed diff. Outside that one change the visual diff should be empty or trivially anti-aliasing noise; if the diff shows any OTHER color shift, that is a BUG, not an intended re-baseline (see stopping rules).

OUT OF SCOPE (do not do these here):
- Applying the dark-fantasy aesthetic to any specific window or chrome (per-surface passes are Phase 7 and Phases 9-18).
- Theme variants high-contrast / colorblind / Okabe-Ito (Phase 21).
- Wiring --text-scale to any UI control or Options slider (Phase 21); only scaffold the multiplier so calc() consumers can reference it.
- Any recolor of icons or any window; any HudContext / a11y utility (Phase 3); any IWorld / sim / server change (none expected, ever, in this packet).

STEP 3 - VALIDATION + MULTI-AGENT REVIEW (run the "Visual/token change" row of the state.md matrix, then the dispatch rule):
1. Type + unit: `npx tsc --noEmit` then `npx vitest run tests/tokens.test.ts tests/hud_harness.test.ts tests/hud_perf_budget.test.ts`. The perf gate must show the hot-write skip rate above ~0.8.
2. i18n guard: `npx vitest run tests/localization_fixes.test.ts` (confirm no new untranslated string slipped in).
3. axe-core a11y assertions: run the packet's axe checks if present (`npx vitest run tests/a11y/*.test.ts` and/or `npx playwright test tests/a11y` if those files exist yet). They may not exist before Phase 3; if absent, note that and rely on the visual + token tests. Tokens themselves carry no new ARIA, so axe is expected clean/unchanged.
4. Playwright visual baseline (DELIBERATE re-baseline): `npx playwright test` first. REVIEW the produced visual diff before updating anything. The ONLY intended pixel change this phase is the .q-* quality-class reconciliation (common -> #ffffff and .q-* pointing at var(--quality-*)); everywhere else the diff should be empty or sub-threshold AA noise. If the diff shows ONLY that reconciliation (and AA noise), run `npx playwright test --update-snapshots`, and state in the final report that the re-baseline was reviewed and shows only the intended .q-* change. If the diff shows ANY OTHER color shift, DO NOT update snapshots: STOP per the stopping rules.
5. Playwright MCP live-game walkthrough: drive a real session (npm run dev, npm run server) via the Playwright MCP and verify: (a) switch locale to a non-English locale and confirm NO English leak introduced by this change; (b) item rarity colors in the DOM (item name text, rarity borders rendered by the .q-* classes) render as expected after the token migration and the .q-* reconciliation (common item names now white); the canvas icon glyphs are unchanged because the painter never read quality color; (c) keyboard-only nav and a visible focus ring still work (this phase changes none of it, so confirm no regression); theme/text-scale/Reader-Mode toggles are not built yet, so just confirm --text-scale resolves to 1 and nothing reads a broken var().
6. A mobile screenshot at a phone viewport (the matrix's mobile screenshot script) to confirm no layout/color regression on touch.
7. REVIEW-DISPATCH (spawn ONLY agents whose surface the diff touches; this is a client-only token change). Per the state.md matrix: spawn `qa-checklist`. Do NOT spawn privacy-security-review (no server/admin/net/SQL/secret/ALLOW_DEV_COMMANDS/sim-RNG touched), do NOT spawn migration-safety (no db path), do NOT spawn cross-platform-sync (no IWorld/sim/wire/RL change). Additionally, for Opus 4.8, spawn ONE fresh subagent to review YOUR diff for correctness, requirement gaps, and that the colors are provably unchanged (not style nits). Prompt every review agent for COVERAGE, not filtering: report all findings, do not pre-judge severity. If a review agent's output is truncated, resume it with: "Continue exactly where you left off; do not repeat earlier findings." Do not commit while any BLOCKING finding is open.

STEP 4 - COMMIT CADENCE (stage ONLY your card's files with explicit paths; NEVER git add -A; push to origin levy-street, never the fork). Suggested headlines (2-5 commits):
- `style(ui): formalize :root into primitive + semantic dark-fantasy tokens` (the index.html / CSS :root layering)
- `feat(ui): migrate QUALITY_COLOR DOM sites to --quality-* tokens with cached readToken` (src/ui/hud.ts DOM color strings + src/ui/tokens.ts; icons.ts only if it still re-exports the map)
- `style(ui): reconcile .q-* quality classes onto --quality-* tokens (common -> white)` (index.html .q-* classes; the one deliberate visual change)
- `feat(ui): scaffold --text-scale multiplier token` (if not folded into the first commit)
- `test(ui): tokens resolve, readToken memoizes, quality mapping intact` (tests/tokens.test.ts)
- `docs(ui): record Phase 1 token ledger and progress` (the progress.md + state.md ledger updates)

STEP 5 - ACCEPTANCE CRITERIA (mirror progress.md "Phase 1"; all must be checked before done):
- [ ] Primitive + semantic CSS custom properties formalized from the existing :root set (existing names preserved as semantic aliases).
- [ ] Dark-fantasy semantic palette (dark slate, restrained gold trim, parchment, crest) defined as tokens, resolving to today's exact colors.
- [ ] QUALITY_COLOR (re-grepped) migrated to --quality-* tokens; the ~8 DOM color-string sites in src/ui/hud.ts read them via a cached readToken() (no per-frame getComputedStyle). The canvas painter is NOT migrated for quality color (it never read it; quality border is CSS outside the painter).
- [ ] The divergent .q-* quality CSS classes (index.html ~:734-738, re-grepped) reconciled onto the single --quality-* tokens, common chosen as #ffffff (classic convention); this one DELIBERATE pixel change re-baselined with a reviewed diff. All other tiers byte-identical to before.
- [ ] --text-scale multiplier scaffold in place (resolvable, not yet wired to UI).
- [ ] tests/tokens.test.ts present and green (tokens resolve; readToken memoizes + invalidates; DOM color strings and .q-* classes resolve to one value per tier); harness + perf gate green (skip rate > 0.8); visuals re-baselined with a reviewed diff that shows ONLY the intended .q-* reconciliation.

STEP 6 - DOC UPDATES + MEMORY:
- progress.md: set Phase 1 row to in-progress then complete with dates; check every Phase 1 deliverable box; add a Notes line describing the token tiers and the readToken helper location.
- state.md: update "Current phase"; fill the (Phase 1) ledger line under "Created by this packet" with the ACTUAL paths/names you shipped (the token block location, the --quality-* token names, the readToken helper path, tests/tokens.test.ts); add any gotcha discovered (e.g. jsdom var() resolution) to OPEN items.
- MEMORY: add a short hud note recording the primitive/semantic token model, the --quality-* token names, the readToken() helper path + its invalidate hook (for Phase 21 theme swap), and the --text-scale scaffold. Keep it terse and index it in MEMORY.md.
- Commit the doc updates with the implementation (explicit paths).

STEP 7 - FINAL RESPONSE FORMAT (return this, concise):
- Status: done / blocked, and the branch name.
- Files touched: absolute paths, grouped (CSS :root, src/ui/icons.ts, new src/ui/tokens.ts, tests/tokens.test.ts, docs).
- Validation results: tsc; the vitest files and their pass counts; perf skip rate value; i18n guard; axe (run or n/a with reason); the Playwright visual outcome (diff reviewed, empty, snapshots updated yes/no); the live-game walkthrough findings (locale no-leak, rarity colors identical, focus ring intact, --text-scale resolves).
- Review verdicts: qa-checklist verdict; the Opus self-review subagent verdict; any BLOCKING resolved.
- Deferrals: anything pushed to a later phase (e.g. theme swap, text-scale wiring) and why.
- One-line handoff to QA (Phase 2): what to verify first (colors provably unchanged; readToken cached; --text-scale resolvable).

STOPPING RULES (stop and surface; do not force through):
- STOP if tokenizing QUALITY_COLOR would change any rarity color BEYOND the single agreed .q-* reconciliation (common -> #ffffff). Every other tier and every surface MUST stay visually identical until a later pass deliberately restyles; this phase is token PLUMBING plus that one reconciliation, not a recolor. If your visual diff shows any OTHER rarity or surface color shift, do not update snapshots and do not commit a recolor; reconcile to byte-identical first. (Reminder: quality color is a DOM/CSS concern; the canvas painter does not read it.)
- STOP if readToken cannot be cached safely (it must invalidate ONLY on a theme change and NEVER run getComputedStyle on the per-frame path). readToken feeds the JS-built DOM color strings; if the only way to set those strings is a per-frame getComputedStyle, that violates invariant 5; surface it rather than ship it.
- STOP if the pre-flight shows a dirty shared worktree you did not create, or if any change would require touching IWorld / src/sim / server (none should), or if a structural DOM id would need renaming to land the tokens.
- STOP if tests/hud_perf_budget.test.ts skip rate drops to or below ~0.8, or if the i18n guard or tsc go red and cannot be made green within the scope above.
```
